Fixes
-----

* fixed the fields command in the interactive console (closes :issue:`176`).

* if there is only one entity defined, start the interactive console in that
  entity, even if default_entity is not set (closes :issue:`175`).