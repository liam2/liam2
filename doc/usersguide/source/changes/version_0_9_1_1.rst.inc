Fixes
-----

* fixed version number (release 0.9.1 reports itself as 0.9).

* fixed various small problems with the release script.

* made the 32b version use the exact same versions of third-party packages
  as the 64b version.